diff --git a/screen.h b/screen.h
index be74838..4a903d6 100644
--- a/screen.h
+++ b/screen.h
@@ -415,6 +415,14 @@
 	void register_vblank_callback(vblank_state_delegate vblank_callback);
 	void register_screen_bitmap(bitmap_t &bitmap);
 
+// DAV HACK
+	// netplay rollback: re-arm the one-shot VBLANK timers after a deferred
+	// savestate reload (the rollback capture point runs inside vblank_begin,
+	// before these timers are re-armed, so a clean-boundary load restores them
+	// disabled and the screen would never produce another frame).
+	void netplay_rearm_vblank();
+// END DAV HACK
+
 	// internal to the video system
 	bool update_quads();
 	void update_burnin();
